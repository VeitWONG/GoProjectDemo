package main

// 主函数
func main() {

	//httpServe.HttpServerStart()

	// 获得数据库引擎
	// engine, _ := database.Conn()
	// database.GetDatabaseInfo(engine)
	// engine.Close()

	/*
		// 声明并初始化User对象
		u1 := model.User{}

		// 结构体中将属性共有，直接获取用户输入进行赋值
		fmt.Print("请请输入姓名,年龄,性别,邮箱(内容之间以空格分割):")
		fmt.Scanln(&u1.Name, &u1.Age, &u1.Gender, &u1.Email)

		fmt.Printf("姓名:%s\n年龄:%d\n性别:%d\n邮箱:%s", u1.Name, u1.Age, u1.Gender, u1.Email)
		database.DBAdd("person", &u1)
	*/

}
